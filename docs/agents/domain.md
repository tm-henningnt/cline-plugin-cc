# Domain docs

This is a single-context repository.

Before exploring or changing a feature, read the root `CONTEXT.md` and any relevant decisions in
`docs/adr/`. Use glossary terms consistently, and surface any contradiction with an ADR rather
than silently overriding it.
